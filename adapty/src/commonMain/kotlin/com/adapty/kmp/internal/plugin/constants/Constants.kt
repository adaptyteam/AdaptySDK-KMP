package com.adapty.kmp.internal.plugin.constants

import kotlin.time.Duration.Companion.seconds

internal object Constants {
    const val DEFAULT_LOCALE = "en"
    val DEFAULT_LOAD_TIMEOUT = 5.seconds
}